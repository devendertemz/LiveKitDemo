<?xml version="1.0" encoding="utf-8"?>
<!-- Testing only: allows plain http:// to the local token server. Do not ship this in a release build. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
